/**
 * 核心闭环集成测试
 * import → read → exit → resume
 */

import { describe, it, expect } from 'vitest';

describe('Resume 闭环', () => {
  // TODO: 实现完整的集成测试
  // 1. 导入一本 txt 文件
  // 2. 打开阅读
  // 3. 模拟翻页到某个位置
  // 4. 退出（触发进度保存）
  // 5. 执行 resume，验证回到同一位置

  it('应定义为占位测试', () => {
    expect(true).toBe(true);
  });
});
